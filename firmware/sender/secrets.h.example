#pragma once

#define WIFI_SSID "your-wifi-name"
#define WIFI_PASSWORD "your-wifi-password"

// Copy the host and token from the existing ui/.env values
#define UPSTASH_REDIS_HOST "your-database.upstash.io" // only hostname not "https://"
#define UPSTASH_REDIS_TOKEN "your-upstash-token"
#define UPSTASH_REDIS_PORT 6379
